# CODEx: URL base del backend para llamadas HTTP desde Next.js
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# CODEx: URL base del WebSocket para notificaciones en vivo
NEXT_PUBLIC_WS_BASE=ws://127.0.0.1:8000
# CODEx: clave pública VAPID utilizada por el service worker
NEXT_PUBLIC_VAPID_PUBLIC_KEY=CAMBIAR_CLAVE_PUBLICA
