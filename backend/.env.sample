# QA: deprecated alias of .env.example – kept for compatibility
# QA: This file contains placeholders only. Never commit real secrets here.

# === Runtime general ===
APP_ENV=staging
DB_CONNECT_TIMEOUT=10
DB_USE_POOL=false  # QA: set DB_USE_POOL=true in prod once pooler verified

# === Supabase / Postgres (no secretos reales) ===
# Direct 5432 (SSL requerido)
SUPABASE_DB_URL=postgresql+psycopg://USER:PASSWORD@HOSTNAME.supabase.co:5432/postgres?sslmode=require
# PgBouncer 6543 (opcional)
SUPABASE_DB_POOL_URL=postgresql+psycopg://USER:PASSWORD@HOSTNAME.supabase.co:6543/postgres?sslmode=require

# === JWT / Auth (placeholders) ===
JWT_SECRET=change_me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# === Redis / WS (si aplica) ===
REDIS_URL=redis://localhost:6379/0

# === Push / VAPID (NO expongas reales) ===
VAPID_PUBLIC_KEY=BB_placeholder
VAPID_PRIVATE_KEY=K_placeholder
VAPID_SUBJECT=mailto:soporte@bullbearbroker.example

# === CORS / API ===
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
API_BASE_PATH=/api
