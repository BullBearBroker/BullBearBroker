# BullBearBroker environment template (root)
# QA: copy backend/frontend examples individually; this file documents structure.
# Backend variables live in backend/.env.example
# Frontend variables live in frontend/.env.example
# Never commit real secrets. Create backend/.env.local and frontend/.env.local locally.
